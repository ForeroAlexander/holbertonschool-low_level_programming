#include "holberton.h"
#include <stdio.h>
#include <stdio.h>

int append_text_to_file(const char *filename, char *text_content)
{
	int c, j, k;

	for (c; c <= NULL || c = 0)
	{

		filename()

	}
		if (c <= NUll);




}
