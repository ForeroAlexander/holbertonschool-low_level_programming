#include "holberton.h"

int _isupper(int C)
{
        if (C >= 'A' && C <= 'Z')
        {
                return (1);
        }
        else
        {
                return (0);
        }
        _putchar('\n');
}
